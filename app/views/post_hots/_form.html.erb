<%= form_with(model: post_hot) do |form| %>
  <% if post_hot.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post_hot.errors.count, "error") %> prohibited this post_hot from being saved:</h2>

      <ul>
        <% post_hot.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_field :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
