<%= form_with(model: post_hottest) do |form| %>
  <% if post_hottest.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post_hottest.errors.count, "error") %> prohibited this post_hottest from being saved:</h2>

      <ul>
        <% post_hottest.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_field :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
