<%= form_with(model: post_hotter) do |form| %>
  <% if post_hotter.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post_hotter.errors.count, "error") %> prohibited this post_hotter from being saved:</h2>

      <ul>
        <% post_hotter.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :body, style: "display: block" %>
    <%= form.text_field :body %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
